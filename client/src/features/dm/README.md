# DM / Direct Messaging Feature

This directory contains all direct-messaging related code.

## Structure
```
features/dm/
├── components/    ← DM-specific UI components
├── hooks/         ← DM-specific custom hooks
└── README.md
```

## What belongs here
- `components/MessageBubble.tsx` (migrate from `src/components/`)
- `components/DMHeader.tsx` (migrate from `src/components/dm/`)
- `components/DMInput.tsx` (migrate from `src/components/dm/`)
- `hooks/useDM.ts` (migrate from `hooks/`)
- `hooks/useDMMedia.ts` (migrate from `hooks/`)
- `hooks/useInboxRealtime.ts` (migrate from `hooks/`)

## Migration Status
> 🚧 Structure stub created. Files pending migration.
