# Live Streaming Feature

This directory contains all live stream related code.

## Structure
```
features/live/
├── components/    ← Live-specific UI components
├── hooks/         ← Live-specific custom hooks
└── README.md
```

## What belongs here
- `components/LiveCommentsPanel.tsx` (migrate from `src/components/live/`)
- `components/LiveLocationMap.tsx`
- `components/LiveSetupView.tsx`
- `components/LiveStreamingOverlay.tsx`
- `components/LiveViewersPanel.tsx`

## Migration Status
> 🚧 Structure stub created. Files pending migration.
