# Stories Feature

This directory contains all story-related code.

## Structure
```
features/stories/
├── components/    ← Story-specific UI components
├── hooks/         ← Story-specific custom hooks
└── README.md
```

## What belongs here
- `components/StoriesRow.tsx` (migrate from `src/components/`)
- `components/StoriesViewer.tsx` (migrate from `src/components/`)
- `components/StoryCommentSection.tsx` (migrate from `src/components/stories/`)
- `components/StoryProgressBars.tsx` (migrate from `src/components/stories/`)
- `hooks/useStories.ts` (migrate from `hooks/`)

## Migration Status
> 🚧 Structure stub created. Files pending migration.
